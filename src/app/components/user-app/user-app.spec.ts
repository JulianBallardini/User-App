import { describe, it, expect } from 'vitest';
import { UserApp } from './user-app';

describe('UserApp Component', () => {
  it('should import UserApp class', () => {
    expect(UserApp).toBeDefined();
  });
});